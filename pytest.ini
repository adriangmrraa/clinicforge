[pytest]
pythonpath = orchestrator_service . whatsapp_service tiendanube_service
asyncio_mode = auto
testpaths = tests
markers =
    integration: tests that require a real Postgres DB (see tests/conftest.py real_db_pool fixture)
    e2e: tests that require DB + real LLM API (manual runs only)
