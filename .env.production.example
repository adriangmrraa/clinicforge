# ============================================
# VARIABLES DE ENTORNO PARA PRODUCCIÓN
# Configuración para deployment en Easypanel
# ============================================

# 1. VARIABLES GLOBALES
INTERNAL_API_TOKEN=clinicforge-internal-token-prod-$(openssl rand -hex 16)
OPENAI_API_KEY=sk-proj-...  # Tu clave real de OpenAI
REDIS_URL=redis://redis:6379
POSTGRES_DSN=postgresql://postgres:$(openssl rand -hex 16)@postgres:5432/clinicforge

# 2. ORCHESTRATOR SERVICE (8000)
BOT_PHONE_NUMBER=+5491111111111  # Tu número real de WhatsApp
CLINIC_NAME=Tu Clínica Dental
CLINIC_LOCATION=Tu Dirección
STORE_WEBSITE=https://tu-dominio-frontend.com
STORE_DESCRIPTION=Descripción de tu clínica

# Cifrado (generar nueva clave para producción)
CREDENTIALS_FERNET_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")

# Seguridad
ADMIN_TOKEN=admin-secret-token-prod-$(openssl rand -hex 16)
JWT_SECRET_KEY=jwt-secret-key-prod-$(openssl rand -hex 32)
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# URLs de producción (¡IMPORTANTE!)
PLATFORM_URL=https://tu-dominio-frontend.com
FRONTEND_URL=https://tu-dominio-frontend.com
CORS_ALLOWED_ORIGINS=https://tu-dominio-frontend.com,http://localhost:3000
CSP_EXTRA_DOMAINS=*.tu-dominio.com

# Configuración de email (para handoff)
NOTIFICATIONS_EMAIL=doctor@tu-clinica.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password
SMTP_SENDER=noreply@tu-clinica.com
SMTP_SECURITY=SSL

# 3. WHATSAPP SERVICE (8002)
YCLOUD_API_KEY=tu-ycloud-api-key-real
YCLOUD_WEBHOOK_SECRET=tu-ycloud-webhook-secret-real

# 4. META ADS (configuración real)
META_APP_ID=tu-meta-app-id-real
META_APP_SECRET=tu-meta-app-secret-real
META_REDIRECT_URI=https://tu-dominio-orchestrator.com/auth/meta/callback
META_ADS_TOKEN=tu-meta-ads-token-real
META_AD_ACCOUNT_ID=act_tu-id-real

# 5. FRONTEND REACT (4173)
VITE_API_BASE_URL=https://tu-dominio-orchestrator.com

# 6. BFF SERVICE (3000)
PORT=3000
ORCHESTRATOR_URL=http://orchestrator_service:8000

# ============================================
# INSTRUCCIONES:
# 1. Reemplazar "tu-dominio-frontend.com" con tu dominio real del frontend
# 2. Reemplazar "tu-dominio-orchestrator.com" con tu dominio real del orchestrator
# 3. Generar claves seguras con openssl rand -hex
# 4. Configurar valores reales para APIs externas
# 5. En Easypanel, crear estas variables en cada servicio
# ============================================